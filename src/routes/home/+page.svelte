<script>
    import { auth } from "../../firebase_loader";
    console.log(auth.currentUser.displayName)
</script>

<div>
    Welcome to SvelteKit! {auth.currentUser.displayName}
    Welcome to SvelteKit! {auth.currentUser.displayName}
    Welcome to SvelteKit! {auth.currentUser.displayName}
    Welcome to SvelteKit! {auth.currentUser.displayName}
    Welcome to SvelteKit! {auth.currentUser.displayName}
</div>